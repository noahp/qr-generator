{
  "name": "qr-generator",
  "version": "1.0.0",
  "description": "Cloudflare Worker that generates QR codes as SVG images",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.46.0"
  },
  "dependencies": {
    "qrcode": "^1.5.3",
    "uqr": "^0.1.2"
  }
}
